RV-0013: Document action-match case-sensitivity behavior

**Problem**
Action matching uses ordinal `Equals` (case-sensitive, no trim). This is likely intended (Google actions are case-sensitive) but is undocumented.

**Location**
`Recaptcha.Verify.Net/VerificationResultValidation/RecaptchaVerificationResultValidationService.cs:34`

**Fix**
- Confirm against the Google spec and document the case-sensitive / no-trim behavior (or decide on normalization).

**Priority:** P2 (API design)
