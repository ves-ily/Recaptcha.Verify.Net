RV-0018: Document OnVerificationFailed delegate signature

**Problem**
The README does not document the `OnVerificationFailed` delegate parameters, so users cannot write the delegate from the docs alone.

**Location**
`README.md:254` vs `RecaptchaAttributeOptions.cs:28`

**Fix**
- Document `(ActionExecutingContext context, string? action, ValidationResult? result)` and what `ValidationResult` carries.

**Priority:** P4 (docs)
