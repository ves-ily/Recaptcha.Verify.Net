RV-0025: Minor cleanups (SecretKey null!, Singleton, dependabot)

**Problem**
Minor cleanups.

**Items**
- `SecretKey = null!` nullable lie (`RecaptchaVerificationOptions.cs:11`) — make `string?` or document the invariant.
- Stateless `Scoped` services (`ConfigurationExtensions.cs:169-171`) could be `Singleton`.
- Add `.github/dependabot.yml` for nuget.

**Priority:** P5 (polish)
