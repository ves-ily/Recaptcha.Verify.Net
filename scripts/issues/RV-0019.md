RV-0019: Add Logging section to README

**Problem**
The README documents tracing but not logging. Consumers do not know which log events/levels to expect, nor that `AddLogging()` is needed in non-ASP.NET hosts.

**Location**
`README.md`; `Recaptcha.Verify.Net/Logging/LoggerExtensions.cs`, `CoreEventId.cs`

**Fix**
- Add a short "Logging" section: `SendingRequest` (Trace), `RequestCompleted`/`ResponseChecked` (Information) + event IDs; note `AddLogging()` for non-ASP.NET hosts.

**Priority:** P4 (docs)
