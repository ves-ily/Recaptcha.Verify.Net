RV-0015: Cover test gaps (BaseUrl e2e, errors, score edge, ordering, timeout)

**Problem**
Several behaviors lack test coverage.

**Location / gaps**
- Custom `BaseUrl` end-to-end (real `HttpMessageHandler` stub asserting `<base>/siteverify` + form fields).
- `VerifyErrorHelper` mapping + unknown-code path.
- Score `>=` boundary and `float.NaN`.
- DI extractor registration order.
- HTTP timeout / cancellation path.

**Fix**
- Add the above tests.

**Priority:** P3 (tests)
