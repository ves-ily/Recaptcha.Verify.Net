RV-0021: CI hardening (scope, matrix, coverage)

**Problem**
CI builds the whole solution (pulling the Angular/npm esproj), never passes `-p:DotNet11SdkAvailable=true`, runs only on Windows, and uploads no test results or coverage.

**Location**
`.github/workflows/build.yml`

**Fix**
- Build library+test projects explicitly (not the solution).
- Pass `-p:DotNet11SdkAvailable=true`; add an Ubuntu matrix.
- Upload TRX + coverlet coverage artifacts.

**Priority:** P5 (infra)
