RV-0010: Validation service concrete class is public (inconsistent)

**Problem**
`RecaptchaVerificationResultValidationService` is `public` while `RecaptchaVerificationService` is `internal` — inconsistent surface (interfaces stay public either way).

**Location**
`Recaptcha.Verify.Net/VerificationResultValidation/RecaptchaVerificationResultValidationService.cs:12`

**Fix**
- Make the concrete validation class `internal`.

**Priority:** P2 (API design)
